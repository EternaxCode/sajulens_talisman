{
  "name": "sajulens-talisman",
  "version": "1.0.0",
  "private": true,
  "description": "Talisman SVG assets for the Sajulens app",
  "scripts": {
    "dev": "npx serve . --listen 3000",
    "validate": "node scripts/validate-svg.mjs",
    "generate-gallery": "node scripts/generate-gallery.mjs",
    "build": "node scripts/validate-svg.mjs && node scripts/generate-gallery.mjs"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.2.0",
    "serve": "^14.2.4"
  }
}
